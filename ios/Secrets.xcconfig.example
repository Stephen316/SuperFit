// Copy to Secrets.xcconfig (git-ignored) and fill in.
// Reference in Info.plist as $(USDA_API_KEY) and read at runtime.
// Open Food Facts needs no key; USDA FoodData Central does (free).
USDA_API_KEY = your_fdc_api_key_here
